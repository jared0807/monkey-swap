export enum Step {
  Details = 'details',
  Confirm = 'confirm',
  Complete = 'complete',
}

export enum FlowType {
  FixedRate = 'fixed-rate',
  Standard = 'standard',
}

export enum ExchangeType {
  Direct = 'direct',
  Reverse = 'reverse',
}
